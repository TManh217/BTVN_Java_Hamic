package Btt1_hamic;

public class b22g {
    public static void main(String[] args) {
        int n = Integer.parseInt(args[0]);
        if (Math.sqrt(n)*Math.sqrt(n) == n) System.out.println("true");
        else System.out.println("false");
   }
}
